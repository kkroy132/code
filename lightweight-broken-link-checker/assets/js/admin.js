/* global jQuery, lwblcAdmin */
( function ( $ ) {
	'use strict';

	if ( typeof lwblcAdmin === 'undefined' ) {
		return;
	}

	$( function () {
		// Progress polling is wired up in a later phase.
	} );
}( jQuery ) );
